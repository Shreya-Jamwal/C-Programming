#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    double a, b, result;
    scanf("%lf %lf", &a, &b);
    result = pow(a, b);
    printf("%.0lf\n", result);
    return 0;
}
