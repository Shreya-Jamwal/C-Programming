#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float P,R,T,CI;
    scanf("%f %f %f ",&P,&R,&T);
    CI=P*pow((1+R/100),  T);
    printf("%.2f",CI-P);
    return 0;
}
