#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N, x;
    int sum = 0;
    scanf("%d", &N);
    while (N!=0){
        x = N%10;
        sum = x + sum;
        N = N/10;
    }
    printf("%d", sum);
    return 0;
}
